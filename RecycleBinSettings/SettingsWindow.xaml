<Window x:Class="RecycleBinTaskbar.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recycle Bin Shortcut Settings"
        Height="280" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Taskbar shortcut language" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,15"
                   Text="Choose the language used for the &quot;Empty Recycle Bin&quot; and &quot;Open Recycle Bin&quot; entries when you right-click the pinned taskbar icon."/>

        <ComboBox Grid.Row="2" x:Name="LanguageComboBox" DisplayMemberPath="NativeName" Height="28" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="3" x:Name="StatusText" VerticalAlignment="Top" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="ApplyButton" Content="Apply" Width="90" Height="30" Margin="0,0,10,0" Click="ApplyButton_Click"/>
            <Button x:Name="CloseButton" Content="Close" Width="90" Height="30" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
